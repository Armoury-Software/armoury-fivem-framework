// TODO: Re-add translation logic

export function Controller(data?: any) {
    return function(target: any) {
        // Reflect.defineMetadata(`export_${propertyKey}`, propertyKey, target);
    }
}
